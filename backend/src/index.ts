// Scientific calculator backend entry point.
//
// HTTP wiring and the safe expression-evaluation API land in later tickets;
// this is the scaffold placeholder so the workspace builds and tests run.
export const SERVICE_NAME = "pdlc-test-backend";
